apiVersion: v1
kind: Service
metadata:
  name: {{ include "fanno-payments.fullname" . }}
  labels:
    {{- include "fanno-payments.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fanno-payments.selectorLabels" . | nindent 4 }}